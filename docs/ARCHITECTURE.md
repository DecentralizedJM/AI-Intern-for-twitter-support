# Twitter Support Bot - Visual Flow Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         TWITTER                                 │
│                                                                 │
│  ┌──────────────┐                    ┌──────────────┐         │
│  │ Public Tweet │                    │ Direct       │         │
│  │ @MudrexHelp  │                    │ Message      │         │
│  └──────┬───────┘                    └──────┬───────┘         │
└─────────┼──────────────────────────────────┼─────────────────┘
          │                                    │
          └────────────────┬───────────────────┘
                           │
                    ┌──────▼──────┐
                    │     n8n     │
                    │  Workflow   │
                    └──────┬──────┘
                           │ HTTP POST
                           │ {username, message, is_dm}
                           │
        ┌──────────────────▼──────────────────┐
        │   PYTHON BACKEND (localhost:8000)   │
        │                                     │
        │  ┌────────────────────────────┐    │
        │  │  1. Intent Classifier      │    │
        │  │     - Gemini AI (opt)      │    │
        │  │     - Keyword fallback     │    │
        │  └───────────┬────────────────┘    │
        │              │                      │
        │  ┌───────────▼────────────────┐    │
        │  │  2. Response Generator     │    │
        │  │     - Template based       │    │
        │  │     - 3-4 variations       │    │
        │  └───────────┬────────────────┘    │
        │              │                      │
        │  ┌───────────▼────────────────┐    │
        │  │  3. Action Router          │    │
        │  └───────────┬────────────────┘    │
        └──────────────┼─────────────────────┘
                       │
          ┌────────────┼────────────┐
          │            │            │
          ▼            ▼            ▼
    ┌─────────┐  ┌─────────┐  ┌─────────┐
    │  Slack  │  │ SQLite  │  │  n8n    │
    │Escalate │  │  Track  │  │Response │
    └─────────┘  └─────────┘  └────┬────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
                    ▼               ▼               ▼
              ┌──────────┐    ┌──────────┐    ┌──────────┐
              │Reply to  │    │ Send DM  │    │   Log    │
              │  Tweet   │    │          │    │          │
              └──────────┘    └──────────┘    └──────────┘
```

## Intent Classification Flow

```
User Message
     │
     ▼
┌─────────────────────────┐
│ Extract Keywords        │
│ - ticket, withdrawal    │
│ - #12345 pattern        │
│ - email/password words  │
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐      ┌──────────────┐
│ Gemini AI Classification│ ◄────┤ If available │
│ (6 intent categories)   │      └──────────────┘
└───────┬─────────────────┘
        │
        ▼
┌─────────────────────────┐
│ Fallback: Keyword Match │
│ (if Gemini unavailable) │
└───────┬─────────────────┘
        │
        ▼
   ┌────────────────────────────┐
   │ Intent Detected:           │
   ├────────────────────────────┤
   │ • new_complaint           │
   │ • has_ticket              │
   │ • dm_ticket_shared        │
   │ • follow_up               │
   │ • credentials_shared      │
   │ • general_question        │
   └────────┬───────────────────┘
            │
            ▼
    Route to Handler
```

## Response Generation Flow

```
Intent + Context
       │
       ▼
┌────────────────────┐
│ Select Template    │
│ from config.py     │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Pick Random        │
│ Variation (1 of 4) │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Fill Placeholders  │
│ e.g. {ticket_num}  │
└────────┬───────────┘
         │
         ▼
  Human-like Response
```

## Escalation Flow (Critical Path)

```
User tweets: "@MudrexHelp withdrawal stuck"
         │
         ▼
Bot replies: "Please email help@mudrex.com"
         │
         ▼
User tweets: "I raised ticket already"
         │
         ▼
Bot replies: "Please DM the ticket number"
         │
         ▼
User DMs: "#12345"
         │
         ▼
┌────────────────────────┐
│ Intent: dm_ticket_shared│
└───────┬────────────────┘
        │
        ├──► Save to Database
        │
        ├──► Generate Response
        │    "Escalated! Team will prioritize"
        │
        └──► Slack Escalation
             ┌──────────────────────────┐
             │ 🚨 Twitter Escalation    │
             │ User: @username          │
             │ Ticket: #12345           │
             │ Time: 2025-11-27 14:30   │
             │ [View Ticket] button     │
             └──────────────────────────┘
```

## Data Flow Diagram

```
┌─────────────┐
│   Tweet     │
│ "Help me!"  │
└──────┬──────┘
       │
       ▼
┌─────────────────────────┐
│ n8n extracts:           │
│ {                       │
│   username: "user123"   │
│   message: "Help me!"   │
│   is_dm: false          │
│ }                       │
└──────┬──────────────────┘
       │ HTTP POST
       ▼
┌─────────────────────────┐
│ Python Backend          │
│ webhook_server.py       │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│ twitter_handler.py      │
│ process_message()       │
└──────┬──────────────────┘
       │
       ├──► gemini_handler.classify_intent()
       │    Returns: "new_complaint"
       │
       ├──► gemini_handler.generate_response()
       │    Returns: "Please email help@..."
       │
       └──► database.save_conversation()
            Stores in SQLite
       
       ▼
┌─────────────────────────┐
│ Returns to n8n:         │
│ {                       │
│   success: true         │
│   intent: "new_complaint"│
│   response: "Please..." │
│   escalated: false      │
│ }                       │
└──────┬──────────────────┘
       │
       ▼
┌─────────────────────────┐
│ n8n posts reply to      │
│ Twitter                 │
└─────────────────────────┘
```

## Database Schema

```
┌──────────────────────────────────────┐
│         conversations                │
├──────────────────────────────────────┤
│ id              INTEGER PRIMARY KEY  │
│ username        TEXT                 │
│ message         TEXT                 │
│ intent          TEXT                 │
│ response        TEXT                 │
│ is_dm           BOOLEAN              │
│ ticket_number   TEXT                 │
│ escalated       BOOLEAN              │
│ created_at      TIMESTAMP            │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│         user_state                   │
├──────────────────────────────────────┤
│ username        TEXT PRIMARY KEY     │
│ last_intent     TEXT                 │
│ ticket_number   TEXT                 │
│ last_interaction TIMESTAMP           │
│ escalation_count INTEGER             │
└──────────────────────────────────────┘
```

## File Structure

```
API Bot/
│
├── Core Logic
│   ├── config.py              # Settings & templates
│   ├── gemini_handler.py      # AI classification
│   ├── twitter_handler.py     # Main processor
│   ├── slack_handler.py       # Escalations
│   └── database.py            # Persistence
│
├── API Layer
│   └── webhook_server.py      # FastAPI endpoint
│
├── Testing
│   ├── test_bot.py           # Interactive CLI
│   ├── demo.py               # Auto demo (interactive)
│   └── demo_auto.py          # Auto demo (no input)
│
├── Configuration
│   ├── .env                  # Secrets
│   ├── .env.example          # Template
│   └── requirements.txt      # Dependencies
│
├── Integration
│   └── n8n_workflow.json     # Workflow template
│
├── Documentation
│   ├── README.md             # Main docs
│   ├── QUICKSTART.md         # Quick setup
│   ├── N8N_SETUP.md          # n8n guide
│   └── PROJECT_SUMMARY.md    # This summary
│
└── Data
    └── conversations.db      # SQLite (auto-created)
```

## Response Template System

```
config.py
    │
    ├── RESPONSE_TEMPLATES = {
    │     "new_complaint": [
    │         "We understand your concern...",
    │         "We hear you...",
    │         "We appreciate you reaching out..."
    │     ],
    │     "has_ticket": [
    │         "Thanks for raising the issue...",
    │         "Got it! Please DM...",
    │         ...
    │     ],
    │     ...
    │   }
    │
    └── Called by: gemini_handler.generate_response()
              │
              └─► Picks random variation
                  Returns human-like response
```

## Deployment Architecture (Future)

```
┌─────────────────────────────────────────────┐
│              Production Setup               │
└─────────────────────────────────────────────┘

Twitter API
     │
     ▼
┌──────────────┐
│  n8n Cloud   │  ($20/month)
│  OR          │
│  DigitalOcean│  ($12/month)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│Railway.app   │  (Free tier)
│Python Backend│  OR Heroku ($7/mo)
└──────┬───────┘
       │
       ├──► Slack Webhook (Free)
       │
       └──► PostgreSQL (Free tier)
            OR SQLite (free)
```

---

**Total Monthly Cost:** $100-132
- Twitter API: $100
- Hosting: $0-32
- Everything else: Free
